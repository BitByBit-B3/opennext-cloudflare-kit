meta {
  name: Get All Users
  type: http
  seq: 1
}

get {
  url: {{apiUrl}}/trpc/user.getAll
  body: none
  auth: none
}

tests {
  test("status code should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("response should have result", function() {
    expect(res.getBody()).to.have.property('result');
  });
}
